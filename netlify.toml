# Netlify configuration for Connections Daily – diag/submit-log

# Static site; we serve the root directory directly with no build.
[build]
  publish = "."
  command = ""

# No redirects or headers in this diagnostic branch.
